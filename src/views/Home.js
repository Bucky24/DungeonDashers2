import React from 'react';

export default function Home() {
    return <div>
        <button>Game (map1)</button>
        <button>Module editor (main)</button>
        <button>Map Editor (map1)</button>
    </div>
}